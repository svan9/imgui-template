#pragma once

#include "config.h"
#include "vec.hpp"

class Buffer {
private:
	uint32* data;
	size_t width, height;
public:
	////////////////////////////////////////////////////////////]
	Buffer();

	////////////////////////////////////////////////////////////]
	Buffer(uint32* data, size_t width, size_t height);

	////////////////////////////////////////////////////////////
	void draw();

	////////////////////////////////////////////////////////////
	void draw(size_t _width, size_t _height);

	////////////////////////////////////////////////////////////
	[[deprecated("todo")]]
	void draw(size_t _x, size_t _y, size_t _width, size_t _height);
};